/* Hide top bar, side panels, ladders, stats */
.header,
.sidebar,
.ladder,
.stats,
footer {
  display: none !important;
}

/* Keep chat visible */
.chat-window,
#messages,
.message {
  display: block !important;
  visibility: visible !important;
  background: #000 !important;
  color: #fff !important;
  font-family: Arial, sans-serif !important;
  font-size: 18px !important;
}

/* Hide usernames, timestamps, buttons */
.username,
.timestamp,
button,
input {
  display: none !important;
}

/* Pure text-only messages */
.message {
  color: #FFD700 !important;
  text-shadow: none !important;
}
