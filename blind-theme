/* === Blind Theme (Chat + Promote/Bias only) === */

/* Hide rankers list and ladder details */
.rankers,
.ladder,
.ladder-details,
.stats,
.sidebar,
.header,
footer {
  display: none !important;
}

/* Keep chat visible */
.chat-window,
#messages,
.message {
  display: block !important;
  visibility: visible !important;
  background: #000 !important;
  color: #fff !important;
  font-family: Arial, sans-serif !important;
  font-size: 16px !important;
}

/* Show promote and bias buttons */
.promote-button,
.bias-button {
  display: inline-block !important;
  visibility: visible !important;
}

/* Optional: simplify chat text */
.username,
.timestamp {
  display: none !important;
}

.message {
  color: #FFD700 !important; /* high contrast gold */
  text-shadow: none !important;
}
