/* === Blind Theme: Blank Promote & Bias buttons === */

/* Hide ladder details and ranker text */
.ladder-details,
.rankers,
.stats,
.ladder-info,
.ladder-text {
  display: none !important;
}

/* Keep chat visible */
.chat-window,
#messages,
.message {
  display: block !important;
  visibility: visible !important;
  background: #000 !important;
  color: #fff !important;
  font-family: Arial, sans-serif !important;
  font-size: 16px !important;
}

/* Buttons visible but completely blank */
.promote-button,
.bias-button {
  display: inline-block !important;
  visibility: visible !important;
  color: transparent !important;   /* hides text */
  text-shadow: none !important;
}

/* Remove any pseudo-content/icons */
.promote-button::before,
.bias-button::before {
  content: none !important;
}
